# DemoServer — MMG Outreach Hub runtime dependencies (Python 3.12)
#
# Install into a dedicated env — see DemoServer/README.md:
#   conda create -n demo python=3.12 && conda activate demo
#   pip install -r DemoServer/requirements.txt
#
# Versions pinned to the known-good set running the live demo.

# --- Web server ---
Flask==3.1.3
flask-cors==6.0.2
# Fast JSON (de)serialization for the PC-adjustment tab's large per-update
# mesh payloads — stdlib json was the dominant cost once everything else
# about that path got fast (see generate_isb_joints.replay_shape).
orjson==3.12.0
# HTTPS is on by default; cryptography generates the self-signed cert.
cryptography
# Advertises the server as mmg-demo.local over mDNS so tablets can use a fixed
# name instead of the DHCP IP (offline-friendly; host OS needs nothing extra).
zeroconf
# doctor.py uses psutil for reliable cross-platform process/port handling
# (optional — doctor.py falls back to OS tools if it's absent).
psutil
# dashboard.py: Material Design theme for the PySide6 control panel.
# (PySide6 itself already comes in via ptb_mmg.)
qt-material
# dashboard.py: pure-Python QR codes for the tablet access / trust URLs.
segno

# --- SSM prediction pipeline ---
# Imported at server startup (generate_isb_joints) and by the headless
# predictor subprocess (predict_headless.py).
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
vtk==9.6.1

# --- Biomechanics / statistical shape-model libraries ---
# Specialised packages; both are published on PyPI.
gias3==3.0.1
ptb_mmg==0.3.25
