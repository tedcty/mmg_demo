.idea/
*.pyc
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist

.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
Demos/SSM Demo/predict_gui/TauriGUI/public/bones.json

# DemoServer runtime output (per-session prediction artifacts)
DemoServer/sessions/

# EMG-game leaderboard scores (runtime data, shared per deployment)
DemoServer/emg_scores.json

# DemoServer TLS cert + private key (auto-generated or dropped in locally)
DemoServer/certs/

# DemoServer double-click launcher (generated per-machine with absolute paths)
DemoServer/run_demo_server.bat
DemoServer/run_demo_server.sh

Demos/Spikerbox/.venv
Demos/SSM Demo/predict_gui/debug
.theia
.claude
Demos/StrangeObjectSegmenter/bones/image/
Demos/StrangeObjectSegmenter/bones/mesh/
Demos/StrangeObjectSegmenter/bones/slices/

# Stray copies of edited files that editor tooling drops relative to whatever
# directory a shell happened to be in, e.g.
#   DemoServer/resources/vendor/Demos/StrangeObjectSegmenter/web/index.html
#   Demos/StrangeObjectSegmenter/bones/DemoServer/server.py
# They are always a nested "Demos/" or "DemoServer/", never the real ones at the
# repo root — the leading **/*/ requires at least one directory in front, so the
# top-level Demos/ and DemoServer/ are untouched.
**/*/Demos/
**/*/DemoServer/
